# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=pipeline_db

# Server Configuration
SERVER_PORT=8080

# Notes:
# - DB_PASS is empty for default XAMPP MySQL installation
# - Change DB_PASS if you have set a MySQL password
# - SERVER_PORT can be changed if 8080 is already in use
